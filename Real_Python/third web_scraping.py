# # MechanicalSoup
# # التي تتيح إنشاء متصفح آلي للتفاعل مع صفحات الإنترنت، مثل تعبئة النماذج وإرسالها.
# import mechanicalsoup
# # ينشئ كائن Browser، وهو المتصفح الذي ستستخدمه لتصفح الإنترنت تلقائيًا وتنفيذ الأوامر (مثل فتح الصفحات، إرسال النماذج، إلخ).
# #  إنشاء متصفح (Browser) والتعامل مع صفحة تسجيل الدخول
# browser = mechanicalsoup.Browser()
# # يُخزّن عنوان صفحة تسجيل الدخول (login) في متغير اسمه url.
# url =  "http://olympus.realpython.org/login"
# # يطلب المتصفح فتح الصفحة الموجودة على الرابط، ويخزّن الرد (الصفحة) في المتغير login_page.
# login_page = browser.get(url)
# # يستخرج كائن soup من login_page، وهو تمثيل HTML يمكن تحليله باستخدام BeautifulSoup.
# # يعني: أصبح بإمكاننا استخدام أوامر soup.select, soup.find, إلخ.
# login_html = login_page.soup
# # يبحث عن العنصر <form> في الصفحة ويأخذ أول نموذج (النموذج الأول الموجود).
# # select("form") تُرجع قائمة بكل النماذج، و[0] يأخذ أول واحد.
# form = login_html.select("form")[0]
# # يحدد أول حقل <input> في النموذج (عادةً يكون اسم المستخدم) ويضع فيه القيمة "zeus".
# form.select("input")[0]["value"] = "zeus"
# # يحدد ثاني حقل <input> في النموذج (عادةً يكون كلمة المرور) ويضع فيه "ThunderDude".
# form.select("input")[1]["value"] = "ThunderDude"
# # يرسل النموذج المعبأ إلى نفس عنوان صفحة تسجيل الدخول (login_page.url).
# # النتيجة تكون الصفحة التي تنتقل إليها بعد تسجيل الدخول (عادةً صفحة المستخدم أو ملفه الشخصي)، ويتم تخزينها في profiles_page.
# # response = browser.submit(form, login_page.url)


# profiles_page = browser.submit(form, login_page.url)
# # طباعة محتوى الصفحة بعد تسجيل الدخول
# print(profiles_page.soup.prettify())
# print(profiles_page.soup.h1.text)

# 🧭 الهدف
# تعلُّم:
# كيف تُرسل نموذج تسجيل دخول من خلال بايثون.
# كيف تتفاعل مع الصفحات التي تُحدّث بياناتها باستمرار (مثل النرد).
# كيف تتعامل مع مواقع تعتمد على المدخلات وليس مجرد تحميل صفحات ثابتة.

import mechanicalsoup
# 1. أنشئ متصفحًا
browser = mechanicalsoup.Browser()
url = "http://olympus.realpython.org/login"
login_page = browser.get(url)
login_html = login_page.soup

# 2. عيّن البيانات في الحقول (اسم المستخدم وكلمة المرور)
form = login_html.select("form")[0]
form.select("input")[0]["value"] = "zeus"
form.select("input")[1]["value"] = "ThunderDude"

# 3. أرسل النموذج
profiles_page = browser.submit(form, login_page.url)

# تحقق من نجاح الدخول
print(profiles_page.url)  # المفترض أن تكون /profiles
# # طباعة محتوى الصفحة بعد تسجيل الدخول
print(profiles_page.soup.prettify())
print(profiles_page.soup.h1.text)
print("Hello")



# ✅ ما تعلمته في هذا الدليل
# 1. طلب صفحات الويب باستخدام urllib
# كيفية إرسال طلب GET للحصول على محتوى صفحة.

# استخدام urllib.request.urlopen() وقراءة المحتوى.

# 2. تحليل صفحات HTML باستخدام Beautiful Soup
# تحويل محتوى HTML إلى كائنات يمكن التنقل بينها.

# استخراج العناصر باستخدام .find()، .select()، وغيرها.

# استخراج النصوص والروابط والعناوين من الصفحات.

# 3. التفاعل مع النماذج باستخدام MechanicalSoup
# إرسال اسم مستخدم وكلمة مرور إلى نموذج تسجيل دخول.

# محاكاة الضغط على زر "Submit".

# التنقل إلى صفحات تتطلب تسجيل دخول.

# 4. قراءة محتوى يتجدد في الزمن الحقيقي
# استخدام .get() داخل حلقة لتحديث البيانات بشكل دوري.

# الانتظار بين الطلبات باستخدام time.sleep().




