# هذا البرنامج يقوم بجلب آخر الأخبار وأول عشر اخبار من الصفحة الرئيسية  من موقع الجزيرة الإخباري
# الخطوات:
# 1. تحديد عنوان الصفحة الرئيسية لموقع الجزيرة
# 2. إرسال طلب للحصول على محتوى الصفحة باستخدام مكتبة requests
# 3. التحقق من نجاح تحميل الصفحة عبر كود الحالة (status code)
# 4. تحليل محتوى الصفحة باستخدام مكتبة BeautifulSoup لاستخراج عناوين الأخبار
# 5. استخراج أول 10 عناوين وروابطها وعرضها مع معالجة الروابط النسبية لتحويلها إلى روابط كاملة

import requests
# جلب الأخبار من موقع الجزيرة
from bs4 import BeautifulSoup
# 1. تحديد عنوان الصفحة الرئيسية للجزيرة
url = "https://www.aljazeera.net/"
# 2. إرسال الطلب للموقع
response = requests.get(url)
# 3. التحقق من نجاح التحميل
if response.status_code == 200:
    soup = BeautifulSoup(response.content, "html.parser")
 # 4. استخدام محدد CSS
    headlines = soup.select(".gc__title a")# محدد لجلب العناوين الرئيسية
    # headlines = soup.select(".article-card__liveblog-title a")# محدد لجلب اخر الاخبار
    print("📰 Ten news from Al Jazeera's homepage:\n")
    # print("Latest News from Al_Jazeera: \n")

    for headline in headlines[:10]:
        title = headline.text.strip()
        link = headline['href']

        if not link.startswith("http"):
            link = "https://www.aljazeera.net" + link

        print(f"- {title}\n  ({link})\n")
else:
    print(f"Failed to load the page. Status code: {response.status_code}")
    


print("___________________________________________________________________________________________________")
#________________________________________________________________________________________________________________        

# هذا البرنامج يقوم بجلب جميع الأخبار من الصفحة الرئيسية لموقع الجزيرة الإخباري
# الخطوات:
# 1. تحديد عنوان الصفحة الرئيسية لموقع الجزيرة
# 2. إرسال طلب للحصول على محتوى الصفحة باستخدام مكتبة requests
# 3. التحقق من نجاح تحميل الصفحة عبر كود الحالة (status code)
# 4. تحليل محتوى الصفحة باستخدام مكتبة BeautifulSoup لاستخراج عناوين الأخبار
# 5. استخراج جميع العناوين التي تحتوي على نص طويل مع روابطها
# 6. إزالة التكرار في العناوين والروابط
# 7. عرض عدد العناوين التي تم جمعها وطباعة العناوين مع روابطها
import requests
from bs4 import BeautifulSoup

# 1. الصفحة الرئيسية لموقع الجزيرة
# تحديد رابط الصفحة التي نريد جلب الأخبار منها
url = "https://www.aljazeera.net/"

# 2. إرسال طلب GET إلى الموقع لجلب محتوى الصفحة
response = requests.get(url)

# 3. التحقق من نجاح تحميل الصفحة (رمز الحالة 200 يعني نجاح)
if response.status_code == 200:
    # 4. تحليل محتوى الصفحة باستخدام BeautifulSoup
    soup = BeautifulSoup(response.content, "html.parser")

    # 5. البحث عن جميع الروابط التي تحتوي على نص طويل (قد تكون عناوين أخبار)
    headlines = []
    for a in soup.find_all("a", href=True):
        # استخراج النص الموجود داخل الرابط مع إزالة الفراغات
        text = a.get_text(strip=True)
        href = a['href']
        # تجاهل النصوص القصيرة جدًا لتفادي الروابط غير المفيدة
        if text and len(text) > 30:
            # إذا كان الرابط نسبيًا، نضيف عنوان الموقع الأساسي لجعله رابط كامل
            if not href.startswith("http"):
                href = "https://www.aljazeera.net" + href
            headlines.append((text, href))

    # 6. إزالة التكرار من قائمة العناوين باستخدام dict.fromkeys للحفاظ على الترتيب
    unique_headlines = list(dict.fromkeys(headlines))

    # 7. طباعة عدد العناوين التي تم جمعها
    print(f"✅ Found {len(unique_headlines)} headlines.\n")

    # 8. طباعة جميع العناوين مع روابطها
    print("📰 All news from Al Jazeera's homepage:\n")
    for title, link in unique_headlines:
        print(f"- {title}\n  ({link})\n")

else:
    # حالة فشل تحميل الصفحة، طباعة رمز الحالة
    print(f"❌ Failed to load the page. Status code: {response.status_code}")


    






